# 🚀 Инструкция по запуску сайта UbonVisa

## Способ 1: Автоматический запуск (рекомендуется)

### Просто запустите файл:
**`запустить_сайт_с_браузером.bat`** - двойной клик по файлу

Батник автоматически:
- Проверит наличие Python или PHP
- Запустит локальный веб-сервер
- Откроет сайт в браузере по адресу `http://localhost:8000`

---

## Способ 2: Ручной запуск через Python

### Если у вас установлен Python:

1. Откройте командную строку (Win + R, введите `cmd`)
2. Перейдите в папку с сайтом:
   ```bash
   cd C:\UbonVisaWebsite
   ```
3. Запустите сервер:
   ```bash
   python -m http.server 8000
   ```
4. Откройте браузер и перейдите по адресу:
   ```
   http://localhost:8000
   ```

### Для остановки сервера:
Нажмите `Ctrl + C` в окне командной строки

---

## Способ 3: Ручной запуск через PHP

### Если у вас установлен PHP:

1. Откройте командную строку
2. Перейдите в папку с сайтом:
   ```bash
   cd C:\UbonVisaWebsite
   ```
3. Запустите сервер:
   ```bash
   php -S localhost:8000
   ```
4. Откройте браузер по адресу `http://localhost:8000`

---

## Способ 4: Прямое открытие (без сервера)

### Просто откройте файл:
Двойной клик по **`index.html`**

⚠️ **Внимание:** Некоторые функции могут не работать без локального сервера (например, AJAX запросы)

---

## 📋 Требования

### Для работы через сервер нужен один из:
- **Python 3.x** - [Скачать](https://www.python.org/downloads/)
- **PHP** - [Скачать](https://www.php.net/downloads.php)
- Или просто используйте батник - он сам найдет что использовать!

### Для прямого открытия:
- Любой современный браузер (Chrome, Firefox, Edge, Safari)

---

## 🔧 Устранение проблем

### Порт 8000 занят?
Измените порт в батнике или команде:
```bash
python -m http.server 8080
```
Тогда адрес будет: `http://localhost:8080`

### Браузер не открывается автоматически?
Откройте браузер вручную и введите адрес:
```
http://localhost:8000
```

### Ошибка "Python не найден"?
1. Установите Python с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Или используйте способ 4 (прямое открытие файла)

---

## 📱 Просмотр на мобильном устройстве

Если ваш компьютер и телефон в одной Wi-Fi сети:

1. Узнайте IP-адрес вашего компьютера:
   ```bash
   ipconfig
   ```
   Найдите "IPv4 Address" (например, 192.168.1.100)

2. На телефоне откройте браузер и введите:
   ```
   http://192.168.1.100:8000
   ```

---

## ✅ Готово!

После запуска вы увидите красивый сайт-визитку UbonVisa с:
- Информацией об услугах
- Контактной формой
- Адаптивным дизайном
- Плавными анимациями

**Приятного использования!** 🎉

